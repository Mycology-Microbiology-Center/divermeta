% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_abundance.R
\name{visualize_abundance}
\alias{visualize_abundance}
\title{Visualize an abundance matrix as a sized-tile plot}
\usage{
visualize_abundance(
  abund,
  feature.labels = NA,
  sample.labels = NA,
  clabel.row = 1,
  clabel.col = 1,
  csize = 1,
  clegend = 1,
  grid = TRUE,
  transform = c("identity", "log1p", "sqrt")
)
}
\arguments{
\item{abund}{A numeric matrix or \code{data.frame} with features in rows and
samples in columns.}

\item{feature.labels}{Optional character vector of row labels (features).
If \code{NA} (default), labels are taken from \code{rownames(abund)} when available,
otherwise sequential indices are used.}

\item{sample.labels}{Optional character vector of column labels (samples).
If \code{NA} (default), labels are taken from \code{colnames(abund)} when available,
otherwise sequential indices are used.}

\item{clabel.row}{Numeric multiplier for the relative size of y-axis text.}

\item{clabel.col}{Numeric multiplier for the relative size of x-axis text.}

\item{csize}{Numeric multiplier controlling the maximum symbol (tile) size.}

\item{clegend}{If greater than 0, a size legend is shown; otherwise the
legend is hidden.}

\item{grid}{Logical; whether to draw a light grid behind the tiles.}

\item{transform}{One of \code{"identity"}, \code{"log1p"}, or \code{"sqrt"} indicating
a simple non-negative transform applied to the abundances for sizing.}
}
\value{
A \code{ggplot2} object representing the visualization.
}
\description{
Creates a compact visualization of an abundance matrix where each cell is
drawn as a square whose area is proportional to the abundance value.
Rows are features (OTUs, species, genes, etc.) and columns are samples.
}
\details{
Values are visualized by magnitude; larger values produce larger
squares. The first row (first feature) is displayed at the top. When
\code{transform = "log1p"} or \code{"sqrt"}, non-negative guards are applied via
\code{pmax(0, x)} prior to the transform. Legend tick labels are shown on the
original (back-transformed) abundance scale for readability.
}
\examples{
set.seed(1)
abund <- matrix(  
  data = floor(rlnorm(n = 5 * 4, meanlog = 1, sdlog = 2.5)),
  nrow = 5, ncol = 4)
rownames(abund) <- paste0("feat", 1:5)
colnames(abund) <- paste0("S", 1:4)
visualize_abundance(abund)

}
